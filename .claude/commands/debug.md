---
description: Hand a failing test or error to the debugger subagent.
argument-hint: <error message, failing test, or symptom>
---

Use the `debugger` subagent to diagnose:

$ARGUMENTS

The agent must reproduce the bug before proposing a fix. If reproduction fails, the agent reports that and stops. Do not skip to fixes.
