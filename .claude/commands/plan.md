---
description: Enter plan mode and route the request to the planner subagent.
argument-hint: <feature or change description>
---

Use the `planner` subagent to break down this change before any code is written:

$ARGUMENTS

Do not write code. Do not edit files. Produce a plan in the format the planner specifies, then stop and wait for approval.
